
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Объект.SwaggerJson = Параметры.SwaggerJson;  
	МакетSwagger 				= РеквизитФормыВЗначение("Объект").ПолучитьМакет("озSwaggerClient"); 
	Объект.HTMLSwaggerКлиент 	= СтрЗаменить(МакетSwagger.ПолучитьТекст(), "#SwaggerJson", Объект.SwaggerJson);

КонецПроцедуры
